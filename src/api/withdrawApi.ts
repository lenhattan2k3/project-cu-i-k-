// File: src/api/withdrawApi.js
/*import axios from "axios";

const API_URL = "http://localhost:3000/api/withdraw"; // đổi theo BE của bạn

// 🟢 Gửi yêu cầu rút tiền
export const requestWithdraw = async ({ partnerId, amount }) => {
  const res = await axios.post(`${API_URL}/create`, {
    partnerId,
    amount,
  });
  return res.data;
};

// 📜 Lịch sử rút tiền theo partnerId
export const getWithdrawHistory = async (partnerId) => {
  const res = await axios.get(`${API_URL}/history/${partnerId}`);
  return res.data;
};
*/